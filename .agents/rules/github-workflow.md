# GitHub Workflow Rule

- **Do not automatically push or upload code to GitHub.** 
- The user uses the local environment for staging and testing.
- Only push to GitHub when the user explicitly requests it.
